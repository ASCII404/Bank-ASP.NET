Public Class WebForm1
    Inherits System.Web.UI.Page

    Protected Sub Page_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load

    End Sub

    Protected Sub CalculateCredit()
        'Dim Annuity As Double
        'Dim parh1 As Double
        'parh1 = Math.Pow(1 + (Integer.Parse(RadioButton1.Text) / 100), dtp_end.Value.Year - dtp_start.Value.Year)
        'Annuity = (Integer.Parse(txtbox_amount.Text) * (Integer.Parse(RadioButton1.Text) / 100) * parh1) / (1 - parh1)
        'MsgBox(Annuity)
    End Sub

End Class